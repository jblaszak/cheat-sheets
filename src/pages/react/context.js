import * as React from "react"
import CheatPage from "../../template/cheatPage"
import { pageData } from "../../data/react/context"

const ReactContextPage = () => {
  return <CheatPage pageData={pageData} />
}

export default ReactContextPage
