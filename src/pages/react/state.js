import * as React from "react"
import CheatPage from "../../template/cheatPage"
import { pageData } from "../../data/react/state"

const ReactStatePage = () => {
  return <CheatPage pageData={pageData} />
}

export default ReactStatePage
